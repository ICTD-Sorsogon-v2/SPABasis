import user from './modules/user';
export default {
    modules: {
        user
    },
    // plugins: [createPersistedState({
    //     key: 'keyname',
    //     storage: window.sessionStorage,
    // })],
}